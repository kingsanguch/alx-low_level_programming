#include "main.h"

/**
 * _puts - prints string followed by new line, to stdout
 * @str: string to print
 *
 */
void _puts(char *str)
{
	char *current = str;

	while (*current != '\0')
	{
		current++;
	}
}
